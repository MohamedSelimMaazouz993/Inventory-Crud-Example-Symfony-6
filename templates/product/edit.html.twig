{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
 <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Project</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_product_index') }}"> 
                    View All Projects
                </a>
            </div>
            <div class="card-body">
                {% include 'product/_form.html.twig' with {'button_label': 'Update Project', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
    
{% endblock %}
